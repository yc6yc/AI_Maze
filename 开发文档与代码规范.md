# AI Maze 开发文档与代码规范

本文档根据当前项目源码整理，覆盖项目任务目标、后端迷宫算法、Boss 战算法、前端通信算法、前端动画算法、前端视觉风格，以及后续开发代码规范。

## 1. 项目任务目标

AI Maze 是一个算法课程设计项目，目标是在战争迷雾限制下控制 AI 角色从起点 `S` 走到出口 `E`，过程中尽量收集金币、规避陷阱、击败 Boss，并通过 `总金币 / 总步数` 或相关统计指标评估策略质量。

核心挑战：

- 地图完整信息不可直接给 Agent 使用，Agent 只能读取已揭露的 `fog_map`。
- 角色每次移动后仅揭露周围 `3x3` 视野。
- 金币 `C/G` 增加收益，陷阱 `T` 扣金币，Boss `B` 需要进入战斗逻辑。
- 出口 `E` 只有在 Boss 清理完成后才算真正胜利。
- 策略需要在探索、收集、Boss 准备、冲刺出口之间做权衡。

评分相关常量：

- 金币价值：`COIN_VALUE = 50`
- 陷阱惩罚：`TRAP_DAMAGE = 30`
- 视野半径：`VIEW_RADIUS = 1`，即 `3x3`
- 最大模拟步数：默认 `500`

主要运行入口：

- 命令行入口：[main.py](main.py)
- FastAPI 后端入口：[api/main.py](api/main.py)
- 服务端驱动前端：[frontend/index.html](frontend/index.html)
- 纯静态可视化前端：[web/index.html](web/index.html)
- Boss 战可视化：[web/boss.html](web/boss.html)

## 2. 项目架构

### 2.1 后端与算法目录

- `core/`：核心数据结构、动作定义、通用寻路、服务端 JSON 适配。
- `agents/`：AI 决策智能体，包括局部贪心、全局规划、全局贪心、组合调度、战斗决策。
- `eval/`：本地离线模拟器和批量评测。
- `api/`：FastAPI 路由层，给前端提供地图、模拟、评测、配置接口。
- `map/`：地图 JSON 文件。
- `tests/`：单元测试。
- `config.json`：算法权重、阈值和模拟参数。

### 2.2 前端目录

项目目前有两套前端：

- `frontend/`：与 FastAPI 同源部署的 Vue3 + Canvas 前端。它通过 `/api/*` 接口实时驱动后端模拟。
- `web/`：纯静态 HTML/CSS/Vanilla JS 可视化。它内置多套前端模拟算法、帧动画、Boss 战页面和算法对比功能。

二者定位不同：

- `frontend/` 更适合作为“真实后端联调界面”。
- `web/` 更适合作为“展示、演示、算法对比、视觉效果页面”。

## 3. 数据结构

### 3.1 地图 JSON

地图文件的核心字段：

```json
{
  "maze": [["#", "S", " ", "E"]],
  "B": [100],
  "PlayerSkills": [[8, 4], [2, 0]],
  "minRouds": 20,
  "CoinConsumption": 5
}
```

字段说明：

- `maze`：二维地图数组。
- `B`：可选 Boss 血量列表，按遭遇顺序使用；如果存在，Boss 数量等于该数组长度。
- `PlayerSkills`：技能列表，每个技能为 `[damage, cooldown]`。
- `minRouds`：Boss 战每次挑战最多攻击轮数。注意项目里沿用输入字段拼写 `minRouds`。
- `CoinConsumption`：Boss 战失败或复活消耗金币。

Boss 血量来源有两条路径：

- 地图 JSON 提供 `B` 数组时，模拟器使用该数组作为完整 Boss 血量序列。
- 地图 JSON 没有 `B` 数组或数组为空时，启动模拟必须通过 API/前端手动传入 `boss_count` 和 `boss_healths`。

无论哪条路径，完整 Boss 数量和未来 Boss 血量都属于服务端模拟器的隐藏信息。Agent 的 `GameContext` 不暴露完整血量数组；只有真正遭遇某个 Boss 时，模拟器才按遭遇顺序读取当前 Boss 血量，并在该次 `boss_event` 中返回当前战斗明细。

地图里的 `B` 格表示 Boss 出现或挑战入口，不表示 Boss 数量。一个 `B` 格可以按血量序列连续刷出多个 Boss；只有所有 Boss 都击败后，出口才会真正解锁。

`boss_event` 需要记录完整挑战过程，包括 Boss 顺序、挑战次数、每回合使用技能与伤害、开战金币、复活/重试消耗、结束金币，以及当前 Boss 是否被击败。

两条 Boss 血量来源互相隔离：地图 JSON 已提供非空 `B` 数组时，手动输入字段会被忽略，不参与校验，也不能覆盖地图血量；只有地图没有非空 `B` 数组时，才会读取和校验手动输入的 Boss 数量与血量。

地图符号：

- `#`：墙，不可通行。
- `S`：起点。
- `E`：出口。
- `B`：Boss。
- `T`：陷阱。
- `C/G`：金币。
- `L`：锁或机关，当前按可行走格处理。
- 空格 `" "`：普通地面。

### 3.2 后端核心模型

定义位置：[core/state.py](core/state.py)

- `Skill`：技能伤害、冷却、剩余冷却。
- `PlayerState`：玩家位置、金币、技能列表、回合数。
- `MazeState`：地图尺寸、`fog_map`、起点终点、已触发陷阱。
- `Action`：移动方向 `UP/DOWN/LEFT/RIGHT/STAY` 和可选技能编号。
- `GameContext`：每回合传给 Agent 的完整受限上下文。

关键约束：

- `fog_map[r][c] = None` 表示未探索。
- `MazeState.is_walkable()` 将 `None` 视为不可走。
- Agent 不应读取完整地图，只能基于 `ctx.maze.fog_map` 决策。

## 4. 后端运行流程

### 4.1 命令行模拟流程

入口：[main.py](main.py)

流程：

1. 读取 `config.json`。
2. 使用 `map_loader.resolve_map_path()` 查找地图文件。
3. `LocalSimulator.from_json()` 创建模拟器。
4. 创建 `CompositeAgent`。
5. 循环调用 `agent.decide(ctx)` 获取动作。
6. 模拟器执行 `_step(action)`，更新玩家、金币、视野、历史。
7. 达到出口、失败或超过最大步数后输出统计。

### 4.2 FastAPI 模拟流程

入口：[api/main.py](api/main.py)

主要接口：

- `GET /api/maps`：列出地图。
- `POST /api/maps/upload`：上传地图。
- `POST /api/sim/start`：创建模拟会话。
- `POST /api/sim/{sid}/step`：后端执行一步 AI 决策。
- `POST /api/sim/{sid}/run`：自动跑完整局。
- `DELETE /api/sim/{sid}`：删除会话。
- `POST /api/eval/run`：批量评测。
- `GET /api/config`、`PUT /api/config`：读取或更新配置。

`api/routers/sim.py` 使用内存字典 `_sessions` 保存会话：

```python
_sessions[sid] = {"sim": sim, "agent": agent}
```

前端只保存 `session_id`，每次单步请求都让后端继续同一局模拟。

### 4.3 模拟器流程

核心文件：[eval/simulator.py](eval/simulator.py)

`LocalSimulator` 内部持有完整地图 `ground_truth`，但只把受限视野写入 `ctx.maze.fog_map`。

每步流程：

1. Agent 根据 `ctx` 返回 `Action`。
2. 模拟器按动作尝试移动，边界与墙检查以 `ground_truth` 为准。
3. 处理当前格：
   - 金币：`coins += 50`，格子变空。
   - 陷阱：`coins -= 30`，格子变空，记录到 `triggered_traps`。
   - Boss：调用 Boss 战处理器。
   - 出口：Boss 全部清理后胜利。
4. `_reveal_fov(pos)` 揭露当前 `3x3`。
5. 技能冷却推进。
6. 将快照追加到 `ctx.history`。

## 5. 后端走迷宫算法

### 5.1 通用寻路库

位置：[core/pathfinding.py](core/pathfinding.py)

项目提供三种寻路基础算法：

- `bfs()`：无权最短路径，适合找最近前沿。
- `astar()`：曼哈顿启发式，适合已知目标的快速最短路。
- `dijkstra()`：带权最短路，适合把陷阱格设置为高代价。

所有算法都依赖 `maze.is_walkable()` 或 `walkable_override`，因此默认不会穿越未探索的 `None` 区域。

### 5.2 局部贪心算法 LocalGreedyAgent

位置：[agents/local_greedy_policy.py](agents/local_greedy_policy.py)

局部贪心只看当前中心周围 `3x3` 窗口。

决策步骤：

1. 获取当前坐标 `(r, c)`。
2. 扫描周围 8 个格子。
3. 对直接邻居和对角邻居分别处理：
   - 直接邻居 `dist=1`：可走格或未知黑雾格都纳入评分。
   - 对角邻居 `dist=2`：必须已知可走，且至少一个中转格可走。
4. 每个候选格计算收益分。
5. 按分数降序选择第一个正收益目标。
6. 如果目标是对角格，只走向可行中转格。
7. 如果没有正收益，返回 `STAY`，交给组合 Agent 判断是否切全局。

评分公式：

```text
score = (raw_value - dist * step_cost) / dist
```

其中：

- 金币：`raw_value = +coin_value`
- 未触发陷阱：`raw_value = -trap_penalty`
- 普通格：`raw_value = 0`
- 直接未知格：单独给 `explore_bonus`
- 访问过的格：扣 `visited_penalty`
- 回头路：扣 `w_backtrack`

默认参数在 `config.json` 的 `local` 段：

- `coin_value = 50`
- `trap_penalty = 30`
- `step_cost = 10.0`
- `explore_bonus = 8.0`
- `visited_penalty = 3.0`
- `w_backtrack = 2.0`

优点：

- 每步复杂度低，适合快速局部收益判断。
- 对临近金币和未知区域反应快。

局限：

- 已知空地通常是负分，容易不愿意穿越长走廊。
- 只能看局部，无法主动规划远处目标。

### 5.3 全局状态机规划 GlobalPlannerAgent

位置：[agents/global_planner.py](agents/global_planner.py)

全局规划器仍然遵守战争迷雾，只在已知 `fog_map` 中规划。

状态机阶段：

- `EXPLORE`：探索地图，找前沿，顺路发现 Boss。
- `COLLECT`：已发现 Boss 但金币不足，专项收集金币。
- `RUSH_TO_BOSS`：金币和探索条件满足后冲向 Boss。
- `RUSH_TO_EXIT`：Boss 清理后冲向出口。
- `RETRY_COLLECT`：Boss 挑战失败且金币不足时，先补金币再重试。

关键逻辑：

- 探索阶段使用 BFS 找最近前沿格。
- 收集金币和冲 Boss 使用 Dijkstra，并给未触发陷阱增加高权重。
- 冲出口使用 A*，优先最短步数。
- 通过扫描 `fog_map` 维护已知 Boss 位置。
- 通过金币骤降启发式判断 Boss 战失败。

陷阱权重：

```text
普通格权重 = 1.0
未触发陷阱权重 = 1.0 + trap_step_cost
```

默认 `trap_step_cost = 31`，意味着算法宁可多绕一些路，也尽量避免踩陷阱。

### 5.4 直接全局贪心 GlobalGreedyAgent

位置：[agents/global_greedy.py](agents/global_greedy.py)

该算法每回合都基于当前已探索区域重新计算，不保留长路径承诺。

决策步骤：

1. 扫描已知 Boss。
2. 记录当前位置访问次数。
3. 从当前位置运行一次 Dijkstra，得到所有已知可达格的带权距离。
4. 枚举所有候选目标：
   - 金币。
   - Boss。
   - 出口。
   - 前沿格。
5. 对候选目标打分。
6. 选择最高分目标路径的第一步。
7. 下一回合重新评估。

候选评分：

- 金币：

```text
score = coin_value / distance_cost - revisit_penalty
```

- 前沿：

```text
score = (frontier_value + frontier_unknown_weight * unknown_neighbors) / distance_cost - revisit_penalty
```

- Boss：

```text
score = boss_value / distance_cost
```

如果金币足够，额外加分；如果金币不足，按缺口扣分；探索比例太低且金币不足时再扣分。

- 出口：

Boss 清理后才考虑，使用极高 `exit_value` 引导冲刺。

特点：

- 比状态机更灵活，每步重新计算。
- 对局部回访加入惩罚，减少原地打转。
- 不穿越未知黑雾，仍满足受限视野规则。

### 5.5 组合调度 CompositeAgent

位置：[agents/composite_agent.py](agents/composite_agent.py)

默认策略为 `hybrid`，调度优先级：

1. 如果 `CombatAgent.should_fight(ctx)` 为真，进入战斗决策。
2. 如果配置为 `direct_global`，直接使用 `GlobalGreedyAgent`。
3. 如果 `GlobalPlannerAgent` 处于 `RUSH_TO_BOSS` 或 `RUSH_TO_EXIT`，继续全局规划，不被局部打断。
4. 如果全局规划当前主导，但周围出现正收益局部目标，则切回局部贪心。
5. 默认调用 `LocalGreedyAgent`。
6. 如果局部连续返回 `STAY` 达到 `stuck_threshold`，切换到全局规划器。

默认 `stuck_threshold = 2`。

## 6. Boss 战算法

### 6.1 后端 CombatAgent

位置：[agents/combat_agent.py](agents/combat_agent.py)

触发条件：

- 玩家站在 Boss 格子。
- 或玩家与 Boss 相邻。

基础战斗策略：

1. 获取当前冷却为 0 的技能列表。
2. 若没有可用技能，返回 `STAY`，本回合等待。
3. 若有可用技能，选择伤害最高的技能。
4. 返回 `Action(move="STAY", use_skill=best_idx)`。

胜负预判 `can_defeat_in_time()`：

1. 复制技能冷却状态。
2. 模拟 `min_rounds` 轮。
3. 每轮选冷却为 0 且伤害最高的技能。
4. 使用后设置技能冷却。
5. 每轮末尾减少冷却。
6. 累计伤害达到 Boss 血量则认为可击败。

扩展能力：

- `enable_memory=True` 时，`decide_combat_with_memory()` 会记录失败技能序列。
- 下一次重试时，同一回合会尽量避开历史失败中用过的技能。
- 如果所有可用技能都被禁用，再回退到最高伤害贪心。

### 6.2 API 中的 Boss 处理器

位置：[api/routers/sim.py](api/routers/sim.py)

`_make_boss_handler(sim)` 给 API 模拟注入一个确定性 Boss 战处理器：

1. 读取当前 Boss 血量。
2. 使用当前玩家技能冷却状态。
3. 模拟 `min_rounds` 轮。
4. 每轮选择最高伤害可用技能。
5. 若总伤害大于等于 Boss 血量，返回 `1`。
6. 否则返回 `0`。

注意：

- 当前 API 处理器只判断胜负，不完整记录每轮动画。
- 金币扣除、胜败后回到迷宫等逻辑由 `LocalSimulator._handle_boss_pause()` 处理。

### 6.3 前端静态 Boss 模拟

位置：[web/boss.js](web/boss.js)

`web/boss.js` 用于生成 Boss 战动画帧。

核心流程：

1. `normalizeMapData()` 读取 `B`、`PlayerSkills`、`minRouds`、`CoinConsumption`。
2. `buildInitialSkillStates()` 初始化技能冷却。
3. `decideCombat()` 选择冷却为 0 且伤害最高的技能。
4. `buildFrames()` 按 Boss、尝试次数、攻击轮生成动画帧。
5. 每次挑战最多 `minRounds` 轮。
6. 如果未击败 Boss，扣 `CoinConsumption` 并生成 retry 或 failed 帧。

动画帧阶段：

- `approach`：接近。
- `slash`：连续斩击。
- `heavy`：重击蓄力。
- `impact`：命中、伤害数字、震屏。
- `recoil`：Boss 后仰。
- `counter_*`：Boss 反击演出。
- `retreat`：退回。
- `idle`：待机恢复。
- `retry` / `failed`：重试或失败。

当前实现说明：

- `web/app.js` 设计了 iframe 消息：发送 `maze-start-boss-battle`，等待 `maze-boss-ready` 和 `maze-boss-complete`。
- 当前 `web/boss.js` 主要通过 `localStorage` 的 `ai_maze_active_map` 读取地图，并未完整实现 `message` 事件接收与完成回传。
- `frontend/static/js/app.js` 也是通过写入 `localStorage` 后打开 `/web/boss.html?embed=1` 来展示 Boss 动画。

## 7. 前端通信算法

### 7.1 `frontend/` 与后端通信

通信封装位置：[frontend/static/js/api.js](frontend/static/js/api.js)

所有 HTTP 请求集中在 `api.js`：

- `_request(method, path, body)` 统一封装 `fetch`、JSON 序列化和错误处理。
- `getMaps()` 获取地图列表。
- `uploadMap(file)` 上传地图。
- `startSim(map, agent, config)` 开始模拟。
- `stepSim(sid)` 单步模拟。
- `runSim(sid, max_rounds)` 后端自动跑完。
- `deleteSim(sid)` 清理会话。
- `runEval(...)` 批量评测。
- `getConfig()` / `setConfig()` 配置读写。

Vue 主应用位置：[frontend/static/js/app.js](frontend/static/js/app.js)

通信状态机：

1. 页面加载时调用 `loadMaps()`。
2. 用户选择地图和算法后调用 `enterGame()`。
3. `enterGame()` 调用 `startSim()`，保存：
   - `sessionId`
   - 当前 `state`
   - `battleConfig`
4. 单步时调用 `stepOnce()`。
5. 自动播放时使用 `setTimeout` 定时调用 `stepOnce()`。
6. 每次后端返回新状态后推入 `history`，并重绘 Canvas。
7. 如果后端返回 `boss_event`，触发 Boss 提示和 Boss iframe 页面。

该通信方式的特点：

- 前端不直接修改后端模拟状态。
- 后端是权威状态源。
- 前端维护 `history` 仅用于回放和渲染。

### 7.2 `web/` 静态页面通信

`web/index.html` 是无后端页面，通信主要发生在本页面和 Boss iframe 之间。

关键机制：

- 地图加载：可从默认路径 `map/*.json`、用户导入文件、内置默认地图读取。
- Boss 数据传递：
  - `window.localStorage.setItem("ai_maze_active_map", ...)`
  - Boss 页读取同一个 key。
- iframe 设计意图：
  - 父页面通过 `postMessage({ type: "maze-start-boss-battle", payload })` 发起 Boss 战。
  - 子页面应回传 `maze-boss-ready` 和 `maze-boss-complete`。

当前实现应优先按 `localStorage` 链路理解。

## 8. 前端动画算法

### 8.1 `frontend/` Canvas 渲染

位置：[frontend/static/js/app.js](frontend/static/js/app.js)

渲染流程：

1. `calcLayout(canvas, rows, cols)` 根据 Canvas 容器尺寸、设备像素比、地图行列数计算：
   - `tile`
   - `originX`
   - `originY`
   - 画布内部像素尺寸
2. `renderMaze(canvas, fogMap, pos, visited)` 重绘整张迷宫。
3. 遍历每个格子，根据 `fog_map` 决定显示黑雾、墙、地面或道具。
4. 使用 `inkWash()` 径向渐变做水彩晕染效果。
5. 使用 `cellRand()` 保证每个格子的随机纹理稳定，不随帧闪烁。
6. 玩家用霓虹圆点绘制，并叠加发光阴影。

播放流程：

- `togglePlay()` 切换播放状态。
- `_scheduleStep()` 用 `setTimeout` 按速度定时调用 `stepOnce()`。
- `applyState()` 保存状态后调用 `render()`。
- 时间轴拖动时 `seekTo(idx)` 从本地 `history` 取快照重绘，不请求后端。

### 8.2 `web/` 迷宫动画

位置：[web/app.js](web/app.js)

动画状态：

- `state.frames`：每一帧的迷宫状态。
- `state.route`：路径点数组。
- `state.frameIndex`：当前帧。
- `state.animPlayerX/Y`：帧间插值时玩家的浮点坐标。
- `state.activeEffects`：当前播放中的精灵表特效。

播放算法：

1. `startPlayback()` 设置 `playing = true`。
2. `scheduleTick()` 计算下一帧延迟。
3. 如果下一帧位置相同，直接 `setFrame(nextIdx)`。
4. 如果位置变化，调用 `requestAnimationFrame` 进入 `animateStep()`。
5. `animateStep()` 使用缓动公式：

```text
eased = 1 - (1 - t)^3
```

6. 逐帧更新玩家插值坐标并重绘。
7. 到达目标帧后清空插值状态，进入下一次调度。

特效算法：

- `spawnEffect(effectName, pos, scale, frameMs)` 创建特效实例。
- `animateEffects()` 使用 `requestAnimationFrame` 根据 `frameMs` 推进精灵表帧。
- `drawEffects()` 从精灵表中裁剪当前帧绘制到目标格。

Boss overlay 流程：

1. 当前帧含 `bossEncounter` 时暂停迷宫播放。
2. 显示 Boss encounter 提示。
3. 打开 Boss iframe。
4. Boss 页面播放结束后关闭 overlay。
5. 在迷宫格子上播放 `boss_win` 或 `boss_lose` 特效。

### 8.3 `web/` Boss 战动画

位置：[web/boss.js](web/boss.js)

Boss 战不是逐像素物理模拟，而是“战斗结果帧序列 + CSS 动画类”。

流程：

1. `buildFrames()` 生成所有战斗帧。
2. 每个技能攻击会展开成多个近战阶段。
3. `setFrame(index)` 切换当前帧。
4. `draw()` 重绘迷宫背景和角色位置发光。
5. `setActorAnim(frame)` 根据帧阶段切换 DOM class：
   - 玩家接近、斩击、命中、撤退。
   - Boss 待机、受击、蓄力、反击。
6. `frameDelay(frame)` 为不同阶段设置不同最小停留时间。
7. `scheduleTick()` 用 `setTimeout` 推进战斗帧。

视觉事件：

- `frame.shake` 触发 `screen-shake`。
- `frame.flash` 触发技能闪光。
- `impact` 阶段显示伤害数字。
- `retry/failed/defeated` 更新事件流和状态标签。

## 9. 前端视觉风格

当前项目前端整体是“深色赛博霓虹 + 水彩墨迹迷宫”的混合风格。

### 9.1 色彩

主色：

- 深空背景：近黑色 `#05040a`。
- 洋红强调：`#ff2d95`。
- 青色强调：`#00e5ff`。
- 金色收益：`#ffaa00` 或偏暖金币色。
- 红色危险：`#ff3355`。
- 绿色成功：`#44ff88`。

地图格：

- 墙：紫灰、蓝灰渐变。
- 地面：半透明暗紫底色。
- 金币：金色墨迹光斑。
- 陷阱：赭黄色或橙色警示。
- Boss：红色光斑。
- 起点：青色。
- 出口：暖金色。
- 黑雾：接近黑色，只保留淡粒子点。

### 9.2 布局

- `frontend/` 使用固定全屏应用布局：顶部状态栏、中间 Canvas、左右侧边栏、底部控制栏。
- `web/` 使用更完整的演示界面：开始页、迷宫舞台、控制条、指标面板、事件流、Boss overlay。
- Canvas 作为主视觉核心，UI 面板以玻璃拟态和霓虹边框辅助。

### 9.3 动效

- 入口页：扫描线、粒子、角标、标题脉冲。
- 迷宫：玩家平滑移动、路径墨迹、特效精灵表。
- Boss：近战连段、震屏、闪光、伤害数字、失败/胜利提示。
- 音频：`web/app.js` 中实现了 intro、maze、boss BGM 的淡入淡出。

## 10. 代码规范

### 10.1 Python 代码规范

命名：

- 文件名、函数名、变量名使用 `snake_case`。
- 类名使用 `PascalCase`。
- 常量使用 `UPPER_CASE`。
- Agent 类统一以 `Agent` 结尾。

结构：

- 核心数据结构放在 `core/`。
- 决策算法放在 `agents/`。
- 模拟和评测放在 `eval/`。
- Web API 放在 `api/routers/`。
- 不要让 Agent 读取完整地图，只能读取 `GameContext` 中的受限状态。

类型：

- 新增核心函数应添加类型标注。
- 新增共享模型优先使用 `dataclass`。
- 坐标统一使用 `(row, col)`，不要混用 `(x, y)`。

算法：

- 所有路径规划必须通过 `MazeState.is_walkable()` 或明确的受限 `walkable_fn`。
- 未探索格 `None` 不允许作为全局寻路中的可行走格。
- 对陷阱代价、金币价值、阈值类参数优先放入 `config.json`，不要硬编码在多个地方。
- 修改 Boss 决策时，需要同时说明是否影响后端模拟、API 模拟和前端动画模拟。

错误处理：

- API 路由使用 `HTTPException` 返回明确错误。
- 文件读取使用 UTF-8。
- 上传地图至少校验 JSON 合法性，后续可补充字段完整性校验。

测试：

- 修改 `core/pathfinding.py` 后运行 `pytest tests/test_pathfinding.py -v`。
- 修改 `eval/simulator.py` 后运行 `pytest tests/test_simulator.py -v`。
- 修改 `agents/local_greedy_policy.py` 后运行 `pytest tests/test_local_greedy_policy.py -v`。
- 修改 `agents/combat_agent.py` 后运行 `pytest tests/test_combat_agent.py -v`。
- 大改 Agent 调度后运行 `pytest tests/ -v`。

### 10.2 FastAPI 代码规范

- 路由文件按领域拆分：`sim.py`、`maps.py`、`eval.py`、`config.py`。
- 请求体使用 Pydantic `BaseModel`。
- 新增算法必须在 `api/routers/sim.py` 的 `AGENT_REGISTRY` 注册。
- 如果该算法也要参与批量评测，需要同步注册到 `eval/eval_runner.py`。
- API 返回给前端的状态应保持 JSON 可序列化，坐标使用数组或简单对象。

### 10.3 JavaScript 代码规范

适用于 `frontend/` 和 `web/`。

命名：

- 函数使用 `camelCase`。
- 常量使用 `UPPER_CASE`。
- DOM 引用统一放在 `els` 或 Vue data 中。
- 状态集中放在 `state` 或 Vue `data()`，避免散落全局变量。

通信：

- `frontend/` 中不得在 Vue 组件里直接写散落的 `fetch`，统一通过 `frontend/static/js/api.js`。
- 后端返回错误时应展示错误消息，并停止自动播放。
- `session_id` 是后端模拟会话的唯一凭证，重置时应调用 `deleteSim()`。

Canvas：

- 每次绘制前先根据容器重新计算布局。
- 使用 `devicePixelRatio` 保证高清。
- 颜色和格子尺寸不要写死到单个地图大小。
- 随机纹理必须是确定性的，例如使用 `cellRand(r, c, seed)`，避免每帧闪烁。

动画：

- 定时推进逻辑和绘制逻辑分离。
- 位移动画使用 `requestAnimationFrame`。
- 自动播放节奏使用 `setTimeout` 控制速度。
- 切换帧时必须清理不再有效的 timer、RAF、overlay 状态。

Boss iframe：

- 如果继续使用 iframe 消息通信，需要在父页面和 Boss 页面同时维护消息协议。
- 推荐协议字段：
  - `maze-start-boss-battle`
  - `maze-boss-ready`
  - `maze-boss-complete`
- 如果继续使用 `localStorage`，应明确 key 为 `ai_maze_active_map`，并保证写入格式稳定。

### 10.4 CSS 代码规范

- 颜色集中在 `:root` CSS 变量。
- 组件使用语义类名，例如 `.sidebar`、`.panel`、`.metric-tile`。
- 动画使用 `@keyframes`，命名要表达具体用途。
- 响应式布局优先用 Grid/Flex 和 `clamp()`。
- Canvas 尺寸由 CSS 决定显示区域，由 JS 设置内部像素尺寸。
- 避免新增过多互相覆盖的 `style=""`，应优先写入 CSS 类。

### 10.5 文档规范

新增算法时至少补充：

- 算法目标。
- 输入输出。
- 是否遵守战争迷雾。
- 决策流程。
- 评分公式。
- 关键参数。
- 与已有算法的差异。
- 是否需要前端展示或 API 注册。

## 11. 新增算法接入流程

### 11.1 新增后端 Agent

1. 在 `agents/` 下新增算法文件，例如 `my_agent.py`。
2. 继承 `BaseAgent` 并实现 `decide(ctx)`。
3. 只读取 `ctx.maze.fog_map`。
4. 在 `api/routers/sim.py` 的 `AGENT_REGISTRY` 注册。
5. 在 `eval/eval_runner.py` 的 `AGENT_REGISTRY` 注册。
6. 在 `frontend/index.html` 的算法下拉框增加选项。
7. 增加或更新测试。

### 11.2 新增前端展示算法

如果算法只用于 `web/` 静态对比：

1. 在 `web/app.js` 中新增 `simulateXxx(data)`。
2. 返回结构保持一致：

```js
{
  frames,
  route,
  score,
  totalSteps,
  totalValue,
  coins,
  bossDefeated,
  outputResult
}
```

3. 在 `runComparison()` 中加入该算法。
4. 在对比面板中增加卡片。
5. 确认 `frames` 中包含 `pos`、`coins`、`phase`、`event`、`grid`、`revealed` 等渲染字段。

## 12. 需要注意的当前问题

- `eval/eval_runner.py` 的 `AGENT_REGISTRY` 当前没有注册 `global_greedy`，而 API 中已经注册；如果要批量评测该算法，需要补齐。
- `main.py` 命令行模拟使用 `LocalSimulator.from_json()` 默认 Boss 处理器，默认处理器返回 `None` 会暂停模拟；API 路由里注入了真实 Boss 处理器。若命令行也要完整跑 Boss，需要同步注入处理器。
- `web/app.js` 设计了 `postMessage` Boss iframe 协议，但 `web/boss.js` 当前主要通过 `localStorage` 初始化，未完整实现消息接收和完成回传。
- `frontend/index.html` 中有 `viewMode` 绑定，但 `frontend/static/js/app.js` 的 `data()` 当前没有显式声明 `viewMode`。如果继续扩展视图模式，应补齐状态和渲染分支。
- `api/routers/config.py` 使用浅合并，嵌套配置更新时会覆盖整个一级字段。复杂配置更新需要改为深合并。
