# 前端风格迁移对接文档 — AI Maze Visualizer

> 目标：将当前原生 HTML/CSS/JS 前端的设计系统完整迁移到 **Vue 3 CDN + 原生 JavaScript + Canvas** 架构，由 **FastAPI 后端** 托管静态文件。

---

## 一、设计系统核心

### 1.1 设计语言

| 设计概念 | 说明 |
|---------|------|
| **氛围感** | "深空 + 霓虹辉光 + 磨砂玻璃"，黑暗虚空主题 |
| **字体** | 等宽编程风格 `'Courier New', 'Consolas', 'Microsoft YaHei', monospace` |
| **质感** | Frosted glass（`backdrop-filter: blur()`），半透明面板叠加在深空背景上 |
| **辉光系统** | 三种主色调（品红/青/金）的 `text-shadow` + `box-shadow` + 动画呼吸 |
| **视觉层次** | Z-index 分层：背景粒子(3) → 辉光球(4) → 角落线(5) → 扫描线(1) → 色差漂移(2) → 内容(10+) |

### 1.2 CSS 变量表（全局 Token）

```css
:root {
  /* ── 深空背景 ── */
  --void: #05040a;
  --void-deep: #030308;
  --surface: rgba(10, 8, 24, 0.85);
  --surface-hover: rgba(15, 12, 30, 0.92);
  --surface-border: rgba(0, 229, 255, 0.08);

  /* ── 磨砂玻璃 ── */
  --glass: rgba(12, 10, 26, 0.72);
  --glass-border: rgba(0, 229, 255, 0.1);

  /* ── 三主色 ── */
  --magenta: #ff2d95;          /* 品红：标题 / 起点 / 主按钮 */
  --magenta-glow: rgba(255, 45, 149, 0.45);
  --magenta-dim: rgba(255, 45, 149, 0.15);

  --cyan: #00e5ff;             /* 青色：边框 / 出口 / 次要按钮 */
  --cyan-glow: rgba(0, 229, 255, 0.4);
  --cyan-dim: rgba(0, 229, 255, 0.1);

  --gold: #ffaa00;             /* 金色：金币 / 路径 / 警告 */
  --gold-glow: rgba(255, 170, 0, 0.45);
  --gold-dim: rgba(255, 170, 0, 0.12);

  /* ── 辅助色 ── */
  --red: #ff3355;              /* 红色：陷阱 / Boss / 危险 */
  --red-glow: rgba(255, 51, 85, 0.5);
  --green: #44ff88;            /* 绿色：成功 */
  --green-glow: rgba(68, 255, 136, 0.3);
  --amber: #ff7700;            /* 琥珀：警告 */

  /* ── 文字 ── */
  --text: #d8d5e8;
  --text-dim: #7a7590;
  --text-faint: #3e3a52;

  /* ── 字体 ── */
  --font: 'Courier New', 'Consolas', 'Microsoft YaHei', monospace;

  /* ── 构图 ── */
  --safe: max(16px, 2.5vw);

  /* ── 动效 ── */
  --transition-out: 320ms cubic-bezier(0.4, 0, 0.2, 1);
  --transition-in: 200ms cubic-bezier(0, 0, 0.2, 1);
}
```

---

## 二、页面布局结构

### 2.1 整体布局

```
┌──────────────────────────────────────────────────────────────┐
│  body::before (深空渐变背景) + #void-bg + #scanlines + #chroma │
│  glow-orb × 3 + corner × 4 + particles                      │
├──────────────────────────────────────────────────────────────┤
│  #introScreen (开场覆盖层，加载后淡出)                         │
│    ├── .intro-grid-bg (地图预览网格动画)                      │
│    ├── .intro-content (标题/按钮)                             │
│    └── .map-selector (右上角地图切换)                          │
├──────────────────────────────────────────────────────────────┤
│  main.app-shell (主界面，intro 结束后显示)                     │
│    ├── .topbar-area                                           │
│    │     ├── .brand-block (Logo + 地图名 + 状态)              │
│    │     └── .minimap-wrap (小地图预览)                       │
│    ├── .sidebar.left                                          │
│    │     ├── .sidebar-tab (MENU 触发按钮)                     │
│    │     └── .sidebar-body (指标/视图/地图/对比面板)            │
│    ├── .stage-area (中央画布区)                               │
│    │     ├── .actor-panel.actor-player (Drifter 立绘)         │
│    │     ├── .canvas-stack (Canvas + 空状态 + Boss 提示)      │
│    │     └── .actor-panel.actor-boss (Judgement 立绘)         │
│    └── .sidebar.right                                         │
│          ├── .sidebar-tab (INFO 触发按钮)                     │
│          └── .sidebar-body (瓦片详情/图例/事件流)              │
├──────────────────────────────────────────────────────────────┤
│  #compareOverlay (对比结果弹层)                                │
└──────────────────────────────────────────────────────────────┘
```

### 2.2 Vue 3 组件拆分建议

```
App.vue
├── VoidBackground.vue          ← #void-bg, glow-orb × 3, corner × 4, scanlines, chroma, particles
├── IntroScreen.vue              ← #introScreen (开场动画 + 地图选择)
├── MainShell.vue                ← main.app-shell
│   ├── TopBar.vue               ← .topbar-area (Logo, 地图名, 状态)
│   ├── SidebarLeft.vue          ← .sidebar.left (Metrics, View, Map, Compare)
│   ├── StageArea.vue            ← .stage-area
│   │   ├── ActorPanel.vue       ← .actor-panel (Player/Boss 立绘)
│   │   └── MazeCanvas.vue       ← Canvas 绘制 + 播放控制
│   └── SidebarRight.vue         ← .sidebar.right (Tile info, Legend, Events)
└── CompareOverlay.vue           ← #compareOverlay (算法对比结果)
```

---

## 三、关键 UI 组件样式规范

### 3.1 磨砂玻璃面板 (`.panel.glass`)

```css
.panel.glass {
  background: var(--glass);                    /* rgba(12,10,26,0.72) */
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid var(--glass-border);       /* rgba(0,229,255,0.1) */
  border-radius: 8px;
  padding: 14px;
  box-shadow: 0 0 30px rgba(0,229,255,0.04);
}
```

**Vue 模板参考**：
```html
<div class="panel glass">
  <div class="section-title">Title</div>
  <!-- content -->
</div>
```

### 3.2 部分标题 (`.section-title`)

```css
.section-title {
  font-size: 11px;
  letter-spacing: 0.22em;
  color: var(--cyan);
  text-transform: uppercase;
  text-shadow: 0 0 6px var(--cyan-dim);
  margin-bottom: 10px;
}
```

### 3.3 指标网格 (`.metrics-grid`)

```css
.metrics-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 6px;
}
.metric-tile {
  display: flex; flex-direction: column; gap: 2px;
  padding: 8px 10px;
  border-radius: 6px;
  background: rgba(0,0,0,0.2);
  border: 1px solid rgba(0,229,255,0.06);
}
.metric-tile span {
  font-size: 9px; letter-spacing: 0.1em;
  color: var(--text-faint); text-transform: uppercase;
}
.metric-tile strong {
  font-size: 16px; font-weight: 700;
  color: var(--text);
  text-shadow: 0 0 8px rgba(0,229,255,0.1);
}
```

### 3.4 分段控件 (`.segmented-control`)

```css
.segmented-control {
  display: flex;
  border-radius: 6px;
  border: 1px solid var(--glass-border);
  overflow: hidden;
  margin-bottom: 8px;
}
.segment {
  flex: 1; padding: 4px 8px;
  border: none; background: transparent;
  color: var(--text-dim); font-family: var(--font);
  font-size: 10px; letter-spacing: 0.06em;
  cursor: pointer; transition: all 120ms ease;
}
.segment.active {
  background: rgba(0,229,255,0.1);
  color: var(--cyan);
  text-shadow: 0 0 6px var(--cyan-dim);
}
```

### 3.5 按钮体系

```css
/* 主按钮（品红辉光） */
.primary-btn {
  display: flex; align-items: center; justify-content: center;
  gap: 8px; min-height: 36px; padding: 0 18px;
  border-radius: 6px;
  border: 1px solid rgba(255,45,149,0.25);
  background: rgba(255,45,149,0.08);
  color: var(--magenta); font-family: var(--font);
  font-size: 12px; letter-spacing: 0.12em;
  cursor: pointer;
  text-shadow: 0 0 8px var(--magenta-glow);
  box-shadow: 0 0 20px rgba(255,45,149,0.04);
  transition: all var(--transition-in);
}
.primary-btn:hover {
  background: rgba(255,45,149,0.14);
  border-color: var(--magenta);
  box-shadow: 0 0 30px rgba(255,45,149,0.08);
}

/* 次要按钮（青色辉光） */
.secondary-btn {
  display: flex; align-items: center; justify-content: center;
  gap: 6px; min-height: 30px; padding: 0 12px;
  border-radius: 4px;
  border: 1px solid rgba(0,229,255,0.1);
  background: rgba(0,229,255,0.04);
  color: var(--text-dim); font-family: var(--font);
  font-size: 10px; letter-spacing: 0.06em;
  cursor: pointer;
  transition: all 120ms ease;
}
.secondary-btn:hover {
  border-color: rgba(0,229,255,0.25);
  background: rgba(0,229,255,0.08);
  color: var(--cyan);
  text-shadow: 0 0 6px var(--cyan-dim);
}

/* 图标按钮 */
.icon-btn {
  display: grid; place-items: center;
  width: 36px; height: 36px;
  border-radius: 6px;
  border: 1px solid rgba(0,229,255,0.08);
  background: rgba(0,0,0,0.2);
  color: var(--text-dim); font-family: var(--font);
  font-size: 18px; cursor: pointer;
  transition: all 120ms ease;
}
.icon-btn:hover {
  border-color: rgba(0,229,255,0.25);
  color: var(--cyan);
  text-shadow: 0 0 8px var(--cyan-dim);
}

/* 文件导入按钮 */
.file-btn {
  display: flex; align-items: center; justify-content: center;
  min-height: 30px; padding: 0 12px;
  border-radius: 4px;
  border: 1px dashed rgba(0,229,255,0.15);
  background: transparent;
  color: var(--text-dim); font-family: var(--font);
  font-size: 10px; letter-spacing: 0.06em;
  cursor: pointer; transition: all 120ms ease;
}
.file-btn input[type="file"] { display: none; }
.file-btn:hover {
  border-color: rgba(0,229,255,0.35);
  background: rgba(0,229,255,0.04);
  color: var(--cyan);
}
```

### 3.6 开关 (`.toggle-field`)

```css
.toggle-field {
  display: flex; align-items: center; gap: 8px;
  font-size: 10px; letter-spacing: 0.06em;
  color: var(--text-dim); cursor: pointer;
}
.toggle-field input[type="checkbox"] {
  appearance: none; width: 14px; height: 14px;
  border-radius: 3px;
  border: 1px solid rgba(0,229,255,0.2);
  background: rgba(0,0,0,0.3);
  cursor: pointer; transition: all 120ms ease;
}
.toggle-field input[type="checkbox"]:checked {
  background: rgba(0,229,255,0.2);
  border-color: var(--cyan);
  box-shadow: 0 0 6px var(--cyan-dim);
}
```

### 3.7 状态胶囊 (`.status-pill`)

```css
.status-pill {
  display: inline-flex; align-items: center; gap: 6px;
  padding: 2px 10px;
  border-radius: 12px;
  font-size: 10px; letter-spacing: 0.08em;
  color: var(--text-dim);
  border: 1px solid rgba(0,229,255,0.12);
  background: rgba(0,0,0,0.3);
}
.status-pill.accent {
  color: var(--magenta);
  border-color: rgba(255,45,149,0.25);
  text-shadow: 0 0 6px var(--magenta-dim);
}
```

### 3.8 算法对比卡片 (`.compare-card`)

```css
.compare-card {
  padding: 10px 12px;
  border-radius: 6px;
  border: 1px solid rgba(0,229,255,0.08);
  background: rgba(0,0,0,0.2);
  transition: border-color 0.2s;
}
.compare-card.compare-winner {
  border-color: var(--green);
  background: rgba(68,255,136,0.04);
  box-shadow: 0 0 16px rgba(68,255,136,0.08);
}
.compare-card-head {
  display: flex; align-items: center; gap: 10px;
  margin-bottom: 8px;
}
.compare-card-letter {
  display: grid; place-items: center;
  width: 28px; height: 28px;
  border-radius: 6px;
  border: 1px solid rgba(0,229,255,0.15);
  background: rgba(0,229,255,0.06);
  font-size: 13px; font-weight: 700;
  color: var(--cyan);
  text-shadow: 0 0 6px var(--cyan-dim);
}
.compare-card-title {
  font-size: 12px; font-weight: 700;
  color: var(--text);
}
.compare-card-desc {
  font-size: 10px; color: var(--text-dim);
  margin-top: 2px;
}
.compare-badge {
  display: inline-flex; align-items: center; gap: 4px;
  padding: 1px 8px;
  border-radius: 10px;
  background: rgba(68,255,136,0.15);
  border: 1px solid rgba(68,255,136,0.25);
  color: var(--green); font-size: 9px;
  letter-spacing: 0.06em;
  text-shadow: 0 0 4px var(--green-glow);
}
.compare-metrics {
  display: grid; grid-template-columns: 1fr 1fr; gap: 3px 10px;
}
.compare-metric {
  display: flex; justify-content: space-between;
  font-size: 10px; padding: 1px 0;
}
.compare-metric span { color: var(--text-dim); }
.compare-metric strong { color: var(--text); font-weight: 700; }
```

### 3.9 侧边栏系统

```css
.sidebar {
  position: fixed;
  top: 0; height: 100vh;
  z-index: 25;
  display: flex;
  transition: transform 0.35s cubic-bezier(0.4, 0, 0.2, 1);
}
.sidebar.left  { left: 0;  transform: translateX(-300px); }
.sidebar.right { right: 0; transform: translateX(300px); }
.sidebar.open.left  { transform: translateX(0); }
.sidebar.open.right { transform: translateX(0); }

.sidebar-tab {
  width: 36px; height: 100%;
  display: flex; align-items: center; justify-content: center;
  writing-mode: vertical-lr;
  background: rgba(5,4,10,0.9);
  border: 1px solid rgba(0,229,255,0.08);
  cursor: pointer;
  font-size: 11px; letter-spacing: 0.12em;
  color: var(--text-dim);
  transition: all 120ms ease;
  user-select: none;
}
.sidebar-tab:hover {
  color: var(--cyan);
  text-shadow: 0 0 8px var(--cyan-dim);
}

.sidebar-body {
  width: 300px;
  background: rgba(5,4,10,0.85);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(0,229,255,0.06);
  overflow-y: auto;
}
.sidebar-inner {
  padding: 16px;
  display: flex; flex-direction: column; gap: 12px;
}
```

---

## 四、Canvas 绘制规范

### 4.1 画布初始化和尺寸计算

```javascript
// mazeCanvas 挂载为 ref
const fitCanvas = () => {
  const rect = canvasRef.value.getBoundingClientRect();
  const dpr = window.devicePixelRatio || 1;
  const width = Math.max(320, Math.floor(rect.width * dpr));
  const height = Math.max(320, Math.floor(rect.height * dpr));
  canvasRef.value.width = width;
  canvasRef.value.height = height;

  const rows = grid.value.length;
  const cols = grid.value[0].length;
  const pad = Math.max(22 * dpr, Math.min(width, height) * 0.04);
  const tile = Math.floor(Math.min((width - pad * 2) / cols, (height - pad * 2) / rows));
  return { dpr, tile, originX: (width - tile * cols) / 2, originY: (height - tile * rows) / 2 };
};
```

### 4.2 瓦片绘制

```javascript
// 使用 TILE 映射对象，颜色来自 CSS Token
const TILE = {
  '#': { label: 'Wall',  color: '#2d2a4a' },
  ' ': { label: 'Empty', color: '#0f0d1a' },
  'S': { label: 'Start', color: '#ff2d95' },
  'E': { label: 'Exit',  color: '#ffaa00' },
  'B': { label: 'Boss',  color: '#ff3355' },
  'T': { label: 'Trap',  color: '#ff7700' },
  'C': { label: 'Coin',  color: '#ffcc00' },
  'G': { label: 'Gold',  color: '#ffcc00' },
  'L': { label: 'Lock',  color: '#7a7590' },
};

// 迷雾瓦片
if (!revealed) {
  ctx.fillStyle = '#080612';
  ctx.fillRect(x, y, tile, tile);
  ctx.fillStyle = 'rgba(255,255,255,0.04)';
  ctx.fillRect(x + tile*0.18, y + tile*0.18, tile*0.64, tile*0.64);
  return;
}
```

### 4.3 路径绘制

```javascript
// 已走过路径：橙色 (#f2a541)，线宽 3px
// 整体路线：青色透明 (rgba(15,139,141,0.25))，线宽 2px
ctx.strokeStyle = 'rgba(242, 165, 65, 0.88)';  // 已走路径
ctx.lineWidth = Math.max(3, tile * 0.16);
ctx.lineCap = 'round';
ctx.lineJoin = 'round';
```

### 4.4 玩家标记

```javascript
ctx.save();
ctx.shadowColor = 'rgba(242, 165, 65, 0.75)';
ctx.shadowBlur = tile * 0.5;
// 画圆形标记 (Player)
ctx.beginPath();
ctx.arc(x + tile/2, y + tile/2, radius, 0, Math.PI * 2);
ctx.fillStyle = '#0f8b8d';
ctx.fill();
```

---

## 五、FastAPI 集成方案

### 5.1 项目目录结构

```
backend/
├── main.py                  # FastAPI 应用入口
├── static/                  # 前端静态文件（Vue 3 CDN 版本）
│   ├── index.html           # 单文件入口
│   ├── styles.css           # 从 web/styles.css 迁移
│   ├── app.js               # 从 web/app.js 提取核心逻辑
│   └── assets/              # 图片资源
│       ├── player.png
│       ├── boss.png
│       ├── coin.png
│       └── ...
├── api/
│   └── routes.py            # API 路由
└── agents/                  # 复用现有 Python 算法代码
```

### 5.2 FastAPI 入口骨架

```python
# main.py
from fastapi import FastAPI
from fastapi.staticfiles import StaticFiles
from fastapi.middleware.cors import CORSMiddleware

app = FastAPI(title="AI Maze API")

app.add_middleware(CORSMiddleware, allow_origins=["*"], allow_methods=["*"], allow_headers=["*"])

# API 路由
@app.get("/api/maps")
async def list_maps():
    """列出可用地图"""
    return {"maps": ["maze_7_7.json", "maze_15_15_1.json", ...]}

@app.post("/api/run/{algorithm}")
async def run_algorithm(algorithm: str, data: dict):
    """运行指定算法，返回 outputResult"""
    ...

# 静态文件（放在最后）
app.mount("/", StaticFiles(directory="static", html=True), name="static")
```

### 5.3 Vue 3 CDN 入口骨架

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>AI Maze Exploration</title>
  <link rel="stylesheet" href="./styles.css">
  <script src="https://unpkg.com/vue@3/dist/vue.global.prod.js"></script>
</head>
<body>
  <div id="app">
    <!-- Vue 组件树，对应 §2.2 的组件结构 -->
  </div>
  <script src="./app.js"></script>
</body>
</html>
```

### 5.4 前后端数据交互

```javascript
// 前端调用后端 API 运行算法
async function runAlgorithm(name, data) {
  const res = await fetch(`/api/run/${name}`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(data)
  });
  return await res.json();  // outputResult 格式
}
```

---

## 六、动效系统

### 6.1 背景粒子系统

```javascript
// 在 VoidBackground.vue 的 onMounted 中初始化
const colors = ['var(--magenta)', 'var(--cyan)', 'var(--gold)'];
const sizes = ['dim', 'normal', 'bright', 'giant'];
const animations = ['drift', 'driftSlow', 'driftFast', 'driftTwinkle'];

function spawnParticle() {
  const p = document.createElement('div');
  p.className = `particle ${sizes[randInt(0,4)]}`;
  p.style.left = `${randInt(0,100)}%`;
  p.style.animationName = animations[randInt(0,4)];
  p.style.animationDuration = `${randInt(8,20)}s`;
  p.style.animationDelay = `${randInt(0,6)}s`;
  p.style.color = colors[randInt(0,3)];
  particleContainer.appendChild(p);
}

// 生成 40 个粒子
onMounted(() => { for (let i = 0; i < 40; i++) spawnParticle(); });
```

### 6.2 开场画面过渡

```css
/* intro → main 过渡 */
.intro-screen { transition: opacity 480ms ease; }
.intro-screen.leaving { opacity: 0; transform: translateY(-8px); }

.app-shell { transition: opacity 420ms ease, filter 480ms ease; }
body.intro-active .app-shell { opacity: 0; filter: blur(12px); pointer-events: none; }
```

### 6.3 开场标题脉动

```css
@keyframes titlePulse {
  0%, 100% {
    text-shadow:
      0 0 8px var(--magenta),
      0 0 24px var(--magenta-glow),
      0 0 60px var(--magenta-dim);
  }
  50% {
    text-shadow:
      0 0 16px var(--magenta),
      0 0 44px var(--magenta-glow),
      0 0 100px var(--magenta-dim),
      0 0 160px var(--magenta-dim);
  }
}
```

---

## 七、迁移检查清单

| # | 项目 | 优先级 |
|---|------|:--:|
| 1 | 完整 CSS 变量迁移到新文件 | 🔴 |
| 2 | `VoidBackground.vue`（背景层：辉光球 + 扫描线 + 粒子） | 🔴 |
| 3 | `IntroScreen.vue`（开场动画 + 地图选择器） | 🔴 |
| 4 | `MazeCanvas.vue`（Canvas 初始化 + 迷雾 + 路径 + 瓦片） | 🔴 |
| 5 | `TopBar.vue`（Logo + 地图名 + 小地图预览） | 🔴 |
| 6 | `SidebarLeft.vue`（指标/视图/地图/对比面板） | 🔴 |
| 7 | `SidebarRight.vue`（瓦片详情/图例/事件流） | 🔴 |
| 8 | `CompareOverlay.vue`（算法对比结果弹层） | 🔴 |
| 9 | `ActorPanel.vue`（Drifter / Judgement 立绘） | 🟡 |
| 10 | `BossBattleOverlay.vue`（Boss 战斗特效） | 🟡 |
| 11 | FastAPI `/api/run/{algorithm}` 端点 | 🔴 |
| 12 | 前端 `fetch()` 调用后端 API | 🔴 |
| 13 | 播放器控制（播放/暂停/进度条/速度） | 🟡 |
| 14 | 地图导入/导出按钮 | 🟡 |
| 15 | localStorage 地图缓存 | 🟢 |

---

## 八、关键注意事项

1. **Canvas 坐标系统**：Canvas 左上角为 (0,0)，瓦片坐标 `(row, col)` 映射到 Canvas 时 `x = originX + col * tile, y = originY + row * tile`
2. **FOV 迷雾**：`fogMap[r][c] === null` 时绘制迷雾瓦片（深色方块），已揭露格子绘制实际内容
3. **路径叠层**：先画完整路径（青色半透明），再画已走路径（橙色不透明），避免 z-fighting
4. **动效性能**：Canvas 绘制走 `requestAnimationFrame`，CSS 动画走 GPU 合成层。避免在 Canvas 循环中触发 DOM 更新
5. **FastAPI 静态文件**：生产环境下建议挂载 nginx 或 CDN 替代 `StaticFiles`
6. **Boss 战斗**：可以保持 Canvas 内联样式触发动画（当前 `boss.js` 的逻辑通过 `iframe` 独立渲染），也可以 Vue 组件化替代
